## Instructions for completing the assignment
* Congratulations, you successfully started the assignment!
* Clone this repo to your local machine (or a hosted service such as Google Colab)
* Answer the questions in the notebook 'Final_Assignment_2026.ipynb'
* Include any additional files (e.g. model checkpoints) that are part of your solution.
* If you use any additional libraries, include them in requirements.txt
* Your notebook should run top-to-bottom (e.g. run 'Restart kernel and run all cells').
* Important: your notebook must contain all outputs and answers. We won't be able to re-run your notebook for you during grading. If outputs are missing we can't properly grade the assignment.
* Push your solution to Github Classroom. Make sure that you commit everything, especially your assignment notebook. As usual:

```
git add -A
git commit -m "commit message"
git push
```


## Setting up your environment
You should be able to run the assignment in the same environment as you use for the labs (e.g. conda with Python 3.14), or create a new one, and install all requirements by running `pip install -r requirements.txt`. If you use any additional libraries, include them in requirements.txt. We should be able to run your notebook in a clean environment based on your requirements list.

When you have set up your environment you can use jupyter lab, VSCode, or your favorite IDE to do the assignment.


### Self-verification
* For this assignment, we don't include unit tests, and there is no automated feedback.
* You are responsible to make sure that your notebook runs top to bottom, that all outputs are in the notebook, and that all required files are included.
* Note that after the deadline, submission to GitHub will no longer be possible.

